동시작업을 의미
특정 로직의 실행이 끝날때 까지 기다리지 않고(전체흐름에 영향을 주지 않고) 나머지 코드를 먼저 실행하는 것
 > ex. setTimeout

 비동기처리되는 유형은 콜백함수 유형을 띈다.
 콜백함수는 각각 독립적인 환경에서 따로 실행된다.

 여러개의 작업을 동시진행할 수 있기때문에 처리속도가 빠른것이 장점.

동기 > 순차작업
비동기 > 비순차작업

promise는 네트워크통신에서 많이 사용되고
비동기처리를 하면서 성공하는 부분'과 실패하는 부분의 처리를 분리하기위해 쓰는 객체이다.

then()catch()finally()
resolve > then > finally
reject > catch > finally
finally 는 둘다 실행